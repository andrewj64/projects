#include<iostream>

encryptcallback(buffer)
{
  //write buffer to encryption file
}

decryptcallback(buffer)
{
  
}

int main()
{
  Crypto * crypto = Fact(encrypt_callback, eSHIFT);

  open("Cleartext file";

  while(!EOF)
  {
	buffer = readbytes;
	crypto.encrypt(buffer);
  }
}
